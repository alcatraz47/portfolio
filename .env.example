# Optional Instagram Graph API token (server-side only, never exposed to browser)
INSTAGRAM_ACCESS_TOKEN=

# Optional override for fields in Graph API query
INSTAGRAM_FIELDS=id,caption,media_type,media_url,thumbnail_url,permalink,timestamp
